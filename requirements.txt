-- Require core runtime by default and optionally hardware deps.
-- See `requirements-core.txt` and `requirements-hw.txt`.

-r requirements-core.txt

# To install hardware-specific packages on a Raspberry Pi (optional):
# pip install -r requirements-hw.txt